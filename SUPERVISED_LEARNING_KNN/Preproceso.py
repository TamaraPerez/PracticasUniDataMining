'''

Descripcion: diseniar un programa que dado un fichero de texto desordene las 
lineas (orden aleatorio)
Input: SampleFile.txt
Output:  SampleFile_shuffled.txt
Objetivo: barajar las muestras (el resultado de esta tarea se requiere en 
la siguiente tarea). Dado un fichero de muestras se requiere un fichero con 
las mismas muestras pero desordenado aleatoriamente. 

'''
class Preproceso:
    '''
    classdocs
    '''


    def __init__(self):
        '''
        Constructor
        '''
        
    def randomizarInstancias(self, instancias):
        #metodo que randomiza la lista de instancias
        instancias.randomize()
        
        